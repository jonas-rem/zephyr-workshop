config APP_SIM_LED
	bool "LED module"
	default y
	depends on GPIO && ZBUS && ZBUS_MSG_SUBSCRIBER

if APP_SIM_LED

module = APP_SIM_LED
module-str = LED
source "subsys/logging/Kconfig.template.log_config"

config APP_SIM_LED_THREAD_PRIORITY
	int "LED thread priority"
	default 5

endif
