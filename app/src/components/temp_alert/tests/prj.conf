CONFIG_ZTEST=y

# Enable temp_alert module
CONFIG_TEMP_ALERT_COMPONENT=y

# Enable ZBus
CONFIG_ZBUS=y

# Enable system workqueue for deferred publishing
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

# Logging (optional, disable for cleaner test output)
CONFIG_LOG=n
CONFIG_PRINTK=n
